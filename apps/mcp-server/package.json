{
  "name": "@kinetics/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace @kinetics/core && tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "test": "npm run build --workspace @kinetics/core && vitest run"
  },
  "dependencies": {
    "@kinetics/core": "file:../../packages/core",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ethers": "6.13.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.8.2",
    "vitest": "^4.1.6"
  }
}
