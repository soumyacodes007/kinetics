{
  "name": "kinetics",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @kinetics/abi && npm run build --workspace @kinetics/core",
    "test": "npm run test --workspace @kinetics/core",
    "generate:abi": "npm run generate --workspace @kinetics/abi"
  }
}
